import React, { Component } from 'react'

export default class Error extends Component {
    state = {
        hasError:false
    }
    static getDerived
    render() {
        if (this.state.hasError) {
            return<h1>kay mahi</h1>
        }
        return (
            <div>
                
            </div>
        )
    }
}
